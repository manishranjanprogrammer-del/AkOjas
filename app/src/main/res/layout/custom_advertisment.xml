<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.android.volley.toolbox.NetworkImageView
        android:id="@+id/imgCustomAdvertisement"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:adjustViewBounds="true"
        android:contentDescription="custom advertisement" />

    <!-- <ProgressBar 
        android:visibility="gone"
        android:id="@+id/progressBarView"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignLeft="@+id/imgCustomAdvertisement"
        android:layout_alignRight="@+id/imgCustomAdvertisement"
        android:layout_alignTop="@+id/imgCustomAdvertisement"
        android:layout_alignBottom="@+id/imgCustomAdvertisement"/> -->

</RelativeLayout>

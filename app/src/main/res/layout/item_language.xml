<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="3dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="@dimen/dimen_0dp"
    card_view:cardBackgroundColor="@color/box_bg_color"
    card_view:cardUseCompatPadding="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="100dp">

        <!-- overlay shown when selected -->
        <View
            android:id="@+id/selectedOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/selected_box_color"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/imgPortrait"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/ic_assamese"
            android:contentDescription="language image"
             />


        <TextView
            android:id="@+id/tvLanguage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="English"
            android:textSize="18sp"
            android:layout_gravity="bottom"
            android:paddingTop="3dp"
            android:paddingBottom="3dp"
            android:background="@color/lang_trans"
            android:textColor="@color/black" />



    </FrameLayout>

</androidx.cardview.widget.CardView>

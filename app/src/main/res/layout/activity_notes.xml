<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <include
        android:id="@+id/appbarAppModule"
        layout="@layout/lay_toolbar_notes"
        android:layout_alignParentTop="true"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/edit_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="@dimen/activity_vertical_margin"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/notes_hint"
        android:maxLength="4000"
        android:textColorHint="@color/hint_color"
        android:textColor="@color/black"
        android:layout_below="@+id/appbarAppModule"
        android:layout_above="@+id/tvQuestionLimit"
        />

    <TextView
        android:id="@+id/tvQuestionLimit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingRight="@dimen/activity_vertical_margin"
        android:clickable="true"
        android:gravity="right"
        android:text="@string/astro_shop_user_question_250"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:layout_alignParentBottom="true"/>

    <ProgressBar
        android:id="@+id/progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_centerInParent="true"/>

</RelativeLayout>
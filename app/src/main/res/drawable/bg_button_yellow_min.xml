<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/currentThemeColor" />
            <stroke android:width="1dp" android:color="@color/currentThemeColor" />
            <corners android:radius="10dip" />
        </shape>
    </item>
    <item>
        <shape>
            <gradient android:angle="270" android:endColor="@color/currentThemeColor" android:startColor="@color/currentThemeColor" />
            <stroke android:width="0dp" android:color="@color/currentThemeColor" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</selector>
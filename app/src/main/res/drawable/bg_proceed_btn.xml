<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/appRippleEffectColor">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/appBtnRippleEffectColor" />
            <corners android:radius="20dp" />
            <stroke android:width="1dp"
                android:color="@color/orange_color" />
        </shape>
    </item>

    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:endColor="@color/backgroundColorView"
                android:startColor="@color/backgroundColorView"
                android:type="linear" />
            <stroke android:width="1dp"
                android:color="@color/orange_color" />
            <corners android:radius="10dip" />
        </shape>
    </item>
</ripple>